import 'package:flutter/material.dart';

class TasksScreen extends StatelessWidget {
  const TasksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    final List<Map<String, dynamic>> networks = [
      {
        "name": "CPX Research",
        "subtitle": "استطلاعات ومهام متنوعة",
        "icon": Icons.public,
        "color": Colors.green,
      },
      {
        "name": "BitLabs",
        "subtitle": "استطلاعات ومهام متنوعة",
        "icon": Icons.science,
        "color": Colors.blue,
      },
      {
        "name": "TheoremReach",
        "subtitle": "استطلاعات ومهام متنوعة",
        "icon": Icons.crop_square_rounded,
        "color": Colors.indigo,
      },
      {
        "name": "Pollfish",
        "subtitle": "استطلاعات سريعة ومجزية",
        "icon": Icons.graphic_eq,
        "color": Colors.red,
      },
      {
        "name": "TapResearch",
        "subtitle": "استطلاعات ومهام متنوعة",
        "icon": Icons.touch_app,
        "color": Colors.pink,
      },
      {
        "name": "AdGem",
        "subtitle": "عروض ومهام متنوعة",
        "icon": Icons.diamond,
        "color": Colors.deepPurple,
      },
      {
        "name": "Kiwiwall",
        "subtitle": "استطلاعات وعروض",
        "icon": Icons.auto_awesome,
        "color": Colors.lightGreen,
      },
      {
        "name": "AdMantum",
        "subtitle": "استطلاعات ومهام متنوعة",
        "icon": Icons.bar_chart,
        "color": Colors.orange,
      },
    ];

    return Scaffold(
      backgroundColor: const Color(0xFF020B1C),
      appBar: AppBar(
        backgroundColor: const Color(0xFF020B1C),
        elevation: 0,
        centerTitle: true,
        title: const Text(
          "الاستطلاعات والمهام",
          style: TextStyle(
            fontWeight: FontWeight.bold,
            color: Colors.white,
          ),
        ),
      ),
      body: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(
          children: [
            const SizedBox(height: 10),

            const Text(
              "✨ اكمل الاستطلاعات واربح المكافآت",
              textAlign: TextAlign.center,
              style: TextStyle(
                color: Colors.white,
                fontSize: 26,
                fontWeight: FontWeight.bold,
              ),
            ),

            const SizedBox(height: 10),

            const Text(
              "اختر أي شبكة لبدء تحقيق المكافآت",
              textAlign: TextAlign.center,
              style: TextStyle(
                color: Colors.white70,
                fontSize: 18,
              ),
            ),

            const SizedBox(height: 25),

            Expanded(
              child: GridView.builder(
                itemCount: networks.length,
                gridDelegate:
                const SliverGridDelegateWithFixedCrossAxisCount(
                  crossAxisCount: 2,
                  mainAxisSpacing: 18,
                  crossAxisSpacing: 18,
                  childAspectRatio: 0.88,
                ),
                itemBuilder: (context, index) {
                  final item = networks[index];

                  return Container(
                    decoration: BoxDecoration(
                      color: const Color(0xFF07152B),
                      borderRadius: BorderRadius.circular(24),
                      border: Border.all(
                        color: Colors.white10,
                      ),
                      boxShadow: [
                        BoxShadow(
                          color: item["color"].withOpacity(0.25),
                          blurRadius: 20,
                          spreadRadius: 1,
                        ),
                      ],
                    ),
                    child: InkWell(
                      borderRadius: BorderRadius.circular(24),
                      onTap: () {
                        // افتح صفحة الشبكة هنا
                      },
                      child: Padding(
                        padding: const EdgeInsets.all(16),
                        child: Column(
                          mainAxisAlignment: MainAxisAlignment.center,
                          children: [
                            Container(
                              width: 72,
                              height: 72,
                              decoration: BoxDecoration(
                                color: Colors.white,
                                borderRadius: BorderRadius.circular(22),
                              ),
                              child: Icon(
                                item["icon"],
                                color: item["color"],
                                size: 38,
                              ),
                            ),

                            const SizedBox(height: 18),

                            Text(
                              item["name"],
                              textAlign: TextAlign.center,
                              style: const TextStyle(
                                color: Colors.white,
                                fontSize: 20,
                                fontWeight: FontWeight.bold,
                              ),
                            ),

                            const SizedBox(height: 10),

                            Text(
                              item["subtitle"],
                              textAlign: TextAlign.center,
                              style: const TextStyle(
                                color: Colors.white70,
                                fontSize: 14,
                              ),
                            ),
                          ],
                        ),
                      ),
                    ),
                  );
                },
              ),
            ),
          ],
        ),
      ),
    );
  }
}